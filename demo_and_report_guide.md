# 発表デモ台本 ＆ レポート用記述ガイド

---

## 🎬 発表会デモ実演シナリオ (所要時間: 約1〜2分)

### 【コンセプト】
「設定ファイルの変更やGUIの操作だけで、Javaコードを1行も書き換えずにアプリケーションの振る舞い（通知先）が動的に拡張される」様子を見せる。

---

### 【デモ手順】

#### 1. 事前準備 (Before状態)
`src/config.properties` を以下のように編集しておきます。
```properties
enabled.plugins=desktop
```

#### 2. 実演 Step 1: 1つのプラグインで起動
GUIを起動します。
```bash
java -cp bin App
```
- **説明セリフ**:
  > 「現在、通知Hubには『デスクトップOS通知』のみが有効化されています。」
- **操作**: メッセージ欄に「テスト」と入力して【送信】ボタンを押す（または `./watch` フォルダ内に適当なファイルをドロップ）。
- **結果**: 画面右下にWindowsのポップアップ通知が表示され、ログに `[Desktop] 成功` と出ます。

#### 3. 実演 Step 2: 設定ファイル変更による機能拡張 (Factory Method のアピール)
アプリを閉じ、`src/config.properties` の設定を変更します。
```properties
enabled.plugins=discord,telegram,desktop
```
アプリを再起動します。
- **説明セリフ**:
  > 「設定ファイルに discord, telegram を追加しただけで、**Javaのソースコードは1行も書き換えずに** 通知先が拡張されました。」
- **操作**: 【送信】ボタンを押す。
- **結果**: 一括して Discord, Telegram, デスクトップ通知へ一斉配信されます。

#### 4. 実演 Step 3: GUIでのリアルタイム着脱 (Observer のアピール)
- **説明セリフ**:
  > 「さらに Observer パターンにより、実行中でもGUIのチェックボックスを操作するだけで、リアルタイムに対象のプラグインを登録・解除できます。」
- **操作**: 「✔ デスクトップOS通知」のチェックを外してから【送信】ボタンを押す。
- **結果**: ログに `[Observer解除]` と表示され、デスクトップ通知のみスキップされて配信されます。

---

## 📝 課題提出レポート用文章 (コピペ・調整用)

### 1. アプリケーションの概要
本システム「通知Hub」は、各種イベント（手動入力やフォルダ内のファイル作成等）の発生を検知し、登録された複数の通知手段（Discord Webhook, Telegram Bot API, OS標準デスクトップ通知など）へ一斉に通知を配信する管理アプリケーションである。本開発では、Java標準ライブラリのみで構築し、GoFのデザインパターンを応用した拡張性の高いアーキテクチャを実現した。

### 2. 採用したデザインパターンとその意図・効果

#### ① Strategy パターン
- **適用箇所**: `plugin.NotificationPlugin` インターフェースおよび各具象クラス
- **導入意図**:
  通知先の追加において `if-else` や `switch` 文で分岐処理を行う従来の実装では、新しい通知先を足すたびに既存コードを修正する必要があり、バグを誘発する恐れがある。
- **効果**:
  `NotificationPlugin` インターフェースで通知送信処理（`sendNotification`）を抽象化することにより、既存のコードに一切手を加えることなく、新しい通知プラグインのクラスを追加するだけで機能拡張が可能となった（**開放閉鎖の原則 / Open-Closed Principle** の実現）。

#### ② Observer パターン
- **適用箇所**: `core.EventManager` (Subject) と `plugin.NotificationPlugin` (Observer)
- **導入意図**:
  通知をトリガーする側（手動入力や `FileWatcher`）が個別通知先の詳細を知る必要をなくし、一括で通知を配信・管理するため。
- **効果**:
  `EventManager` が複数の `NotificationPlugin` を保持・一括呼び出し（`notifyAllPlugins`）する構成とした。これにより、実行時（ランタイム）においてGUI上のチェックボックスから動的にプラグインの登録（`addPlugin`）および解除（`removePlugin`）を行うことが可能となった。

#### ③ Factory Method パターン
- **適用箇所**: `factory.PluginFactory`
- **導入意図**:
  メインプログラム（`App.java`）が各通知プラグインの具象クラス（`DiscordNotifier` や `TelegramNotifier` など）の生成方法に直接依存するのを防ぐため。
- **効果**:
  `config.properties` の設定値（`enabled.plugins`）に基づいて、動的に適切なプラグインオブジェクト群をインスタンス化する設計とした。これにより、設定ファイルを書き換えるだけでアプリケーションの挙動（有効な通知先）を変更・デモンストレーションできるようになった。

---

## 📌 保留中の将来拡張アイデア案

1. **`FileLoggerNotifier` (ローカルログ保存プラグイン)**
   - `logs/notification.log` ファイルに配信ログを自動追記するプラグイン。
2. **`SoundNotifier` (サウンド通知プラグイン)**
   - `java.awt.Toolkit.getDefaultToolkit().beep()` を使用した音響通知。
